# Pinned to the versions the published figures were generated with.
# `pip install -r requirements.txt` reproduces the run exactly; the looser
# floors in pyproject.toml are what the code actually requires.
numpy==2.5.1
pandas==3.0.5
matplotlib==3.11.1
seaborn==0.13.2
scipy==1.18.0
scikit-learn==1.9.0
networkx==3.6.1
statsmodels==0.14.6
pytest==8.4.2
